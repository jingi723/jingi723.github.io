---
title: "다시 처리할 데이터인가, 바로 소비할 데이터인가 — RDB와 S3를 나누는 기준"
date: 2026-06-05 11:00:00 +0900
categories: [data-pipeline, 설계]
tags: [data-pipeline, s3, rdb, data-lake]     # 태그는 항상 소문자로
description: 수집한 데이터를 전부 RDB에 두던 습관을 멘토님 피드백으로 다시 본 기록. 저장소는 "편한 곳"이 아니라 "데이터의 역할"로 정한다.
image:
  path: /assets/img/posts/rdb-vs-s3-data-pipeline/thumbnail.png
  alt: 데이터의 역할에 따라 RDB와 S3 레이크로 저장소를 나누는 결정 흐름
---

처음 데이터 파이프라인을 만들었을 때, 수집한 데이터를 전부 RDB에 넣었다. 뉴스 원문, 가격,
재무제표, 새로 만든 피처까지 전부 테이블에 직접 박았다. 한 곳에 다 있으니까 편하다고
생각했다.

그런데 데이터를 다시 만들어야 하는 순간이 자주 왔다. 정규화 규칙을 손보거나 원천 API가
바뀌면 외부에서 다시 받아 저장하는 흐름을 처음부터 돌렸다. 그때마다 수집 코드를 다시
굴렸고, 이미 RDB에 있던 테이블은 다시 덮어쓰는 식이라 이전 상태가 남아 있지도 않았다.

멘토님에게서 한 줄 피드백을 받았다. 컨슈머가 직접 소비하는 최종 데이터는 RDB에 두고, 그
이전 단계의 원천과 중간 산출물은 S3에 두는 편이 좋겠다는 내용이었다. 처음엔 어차피
데이터인데 어디에 넣든 뭐가 다를까 싶었다. 그런데 우리 파이프라인은 뉴스와 가격을 계속
쌓고 다시 처리해야 하고, **전 종목** 커버리지와 준실시간성을 함께 챙겨야 했다. 그러면
저장소를 정하는 기준은 어디가 편한가가 아니라 **그 데이터가 어떤 역할을 하는가**여야 했다.

## OLTP와 OLAP, ETL과 ELT, 레이크와 웨어하우스 — 저장소를 가르는 축 세 개

멘토님이 던진 말을 내 언어로 풀려면 저장소를 가르는 축 두 개와 업계 용어 하나만 잡으면 됐다.

**OLTP와 OLAP.** OLTP는 지금 발생하는 거래·요청을 한 건 단위로 정확히 처리하는 쪽이고,
OLAP은 쌓인 데이터를 대량으로 스캔·집계하는 쪽이다. 대표 저장소도 각각 행 지향 RDB와
열 지향 분석 DB로 갈린다. 우리 파이프라인이라면 기간별 가격 흐름을 비교하거나 팩터를
계산하려고 전 종목을 훑는 작업은 OLAP 쪽이고, 특정 종목의 설명 결과를 한 건 내려주는 건
OLTP 쪽이다.

**ETL과 ELT.** 원천을 가공해서 적재하면(ETL) 저장소는 깔끔하다. 대신 원천이 남지 않아
로직이 바뀌면 처음부터 다시 만들어야 한다. 원천을 그대로 먼저 적재하면(ELT) 재처리 여지가
생긴다. S3에 원천을 남겨두고 나중에 다시 처리하겠다는 발상은 사실상 ELT다.

**Lake와 Warehouse.** 형식을 가리지 않고 원천과 중간 산출물을 그대로 쌓아 두고 읽을 때
스키마를 입히는(schema-on-read) 쪽이 **Data Lake**고, 적재 시점에 스키마가 정해지고
(schema-on-write) 정형 조회에 최적화된 쪽이 **Data Warehouse**다.

세 개를 겹쳐 보면 그림이 나온다. 원천과 중간 산출물은 schema-on-read로 S3에 남겨 재처리에
열어두고, 최종 마트는 정형 저장소에 모아 컨슈머가 바로 읽게 한다. 멘토님 피드백이 말한 게
정확히 이 그림이었다.

![OLTP↔OLAP · ETL↔ELT · Lake↔Warehouse 세 축으로 저장소를 가르는 인포그래픽](/assets/img/posts/rdb-vs-s3-data-pipeline/storage-axes.png)
_저장소를 가르는 세 축 — 처리 성격, 적재 방식, 저장 형태._

## 다시 처리할 수 있어야 하면 S3, 바로 소비해야 하면 RDB — 두 가지로 자른다

멘토님 피드백의 핵심은 "RDB를 쓰지 말자"가 아니라 **RDB와 S3의 역할을 나누자**는 것이었다.
내가 받아들인 기준은 이거다. **다시 처리할 수 있어야 하는 데이터는 S3에 두고, 바로
소비해야 하는 최종 데이터는 RDB에 둔다.** 한 줄짜리 기준이 데이터를 둘로 가른다.

**S3에는 원천과 정제 결과를 둔다.** 뉴스·가격·재무제표·공시 원본과 그걸 표준 스키마로
정규화한 결과다. 정규화 규칙이나 품질 기준이 바뀌면 과거 구간을 다시 만들어야 하는데,
원본이 남아 있으면 외부 API를 다시 부르지 않고 정제만 다시 돌리면 된다.

**RDB에는 소비되는 데이터를 둔다.** 팩터, 분석 산출물, 설명 결과처럼 분석 엔진과 서빙이
바로 읽는 데이터다. 처음에 나는 "소비자"를 API 서버로만 좁게 봤다. 그런데 **분석 엔진도
소비자다.** 팩터와 피처는 중간 산출물처럼 보이지만 분석 엔진이 곧바로 읽는 입력이라
재처리 대상이 아니라 소비 대상이다. 중간 산출물로 분류해 S3에 두면 읽는 쪽이 매번 파일을
긁어야 한다.

기준이 바뀐 게 아니다. 기준을 읽는 눈이 바뀐 것이다. "바로 소비하는 데이터는 RDB"라는
원칙은 그대로 두고, 소비자가 누구인지를 좁게 본 게 문제였다.

![다시 처리할 데이터는 S3로, 바로 소비할 데이터는 RDB로 가르는 결정 트리](/assets/img/posts/rdb-vs-s3-data-pipeline/storage-decision.png)
_다시 처리할 데이터는 S3, 바로 소비할 데이터는 RDB로 가른다._

## 처음에는 어디에 넣든 크게 다르지 않다고 봤는데, 기준은 데이터의 역할에 있다

처음에는 데이터를 RDB에 잘 넣는 게 파이프라인을 잘 만드는 거라고 생각했다. 하지만
저장소를 고르는 기준은 조회가 편한지가 아니라 그 데이터가 다시 처리될 데이터인지 바로
소비될 데이터인지다.

**다시 처리할 가능성이 있는 데이터는 S3에 남기고, 바로 소비해야 하는 최종 데이터는 RDB에
둔다.** 이 한 줄로 지금까지 쌓은 데이터를 다시 분류했다. 전 종목 커버리지와 준실시간성을
함께 챙기려면 처음부터 역할별로 나눠 두는 편이 확장할 때 덜 복잡하다.

다만 이 기준이 모든 데이터를 깔끔히 갈라주지는 않는다. 재처리 대상과 소비 대상에 동시에
걸리는 데이터가 있는데, 그건 기준만으로 안 풀리고 따로 정해야 했다.

다음 글은 이 기준을 우리 파이프라인에 실제로 앉힌 설계를 다룬다. 수집·정제·분석·서빙을
S3와 RDB에 어떻게 나눴는지, 그리고 양쪽에 걸리는 데이터를 어떻게 정했는지.

## 참고 자료

- [AWS — OLAP와 OLTP의 차이점](https://aws.amazon.com/ko/compare/the-difference-between-olap-and-oltp/)
- [IBM — OLAP vs OLTP](https://www.ibm.com/kr-ko/think/topics/olap-vs-oltp)
- [Snowflake — OLAP vs OLTP](https://www.snowflake.com/ko/fundamentals/olap-vs-oltp-the-differences/)
- [AWS — 데이터 레이크란 무엇인가](https://aws.amazon.com/ko/what-is/data-lake/)
